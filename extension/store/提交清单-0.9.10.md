# 采集助手 0.9.10 更新提交清单

> 这是一次**更新提交**。⚠️ **0.9.8 与 0.9.9 都没提交过商店——直接提交 0.9.10 一步到位**，
> 它包含前两版的全部变更：0.9.8 的公众号风险确认闸、0.9.9 的失败现场截图、0.9.10 的执行可视化。
> 逐项填法沿用 `提交清单-0.9.8.md` 的「隐私权页逐项」那节；本清单只写要点与增量。
> 提交动作只有你能做——需要你本人的 Chrome 开发者账号登录 devconsole。

---

## 0. 要传的文件（已打好）

```
public/downloads/beacon-collector-0.9.10.zip
```

- 版本：**0.9.10**（manifest 已确认）
- 大小：1076 KB
- sha256：`63b93d45acb2a596ddc09aac7be16baeae72ba53a4a62ae5a6666011eab9d3bb`

重新打包命令（改了插件代码后重跑）：`npm run pack:ext`

---

## 1. 三个版本的变更合并说明（给审核备注/自己记）

1. **0.9.8 公众号风险确认闸**：`collectWechatOne` 首次使用需用户单独勾选确认知悉风险，
   未确认一个请求都不发。见 `提交清单-0.9.8.md` 第 1 节。
2. **0.9.9 失败现场截图**：解析失败且发生在用户正看着的页面时，随脱敏骨架附一张压缩截图
   （150KB 上限、30 天服务端自动清空、后台标签页绝不截）。**零新权限**。
3. **0.9.10 执行可视化（默认关）**：设置里开启后，用户当场点「批量采集 / 回填我的数据」时
   工作标签页显示在前台并叠加进度卡（打开 → 翻页 → 解析条数 → 完成）。
   只改变「让用户看见」，不改变采集范围/字段/频率；定时与后台派发不受影响仍全程后台。
   **零新权限**。

权限声明：与在架版本完全一致，零变化。

## 2. 商店侧要同步改的话术

- 隐私权页两处：解析失败截图（30 天删除/后台不截）、执行可视化（默认关/只前台展示过程）。
  两份政策文本已更新（`privacy.md` 与托管页 `privacy-policy.html`——托管页要在
  **服务端部署后**重新跑 `npx tsx scripts/privacy-page.ts` 生成再上传）。
- 其余（截图素材、分类、说明）不变。

## 3. 上架后

```
BEACON_EXT_STORE_CHROME_VERSION="0.9.10"
```
