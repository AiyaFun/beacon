# 更新日志

整机版「检查更新」卡片里显示的更新说明就取自这份文件（`scripts/pack-appliance.ts` 读当前版本那一段的列表项）。
写法：一个版本一个 `## x.y.z (日期)` 二级标题，下面每条 `- ` 就是发给用户看的一句话；
段落与三级标题只给人读，不进清单。版本号升了这里没跟上，打包会被拦下。

## 1.3.73 (2026-09-15)

- 执行记录页：状态标签从彩色药丸改为小圆点+文字，选中行从橙粉底色改为中性灰底+左侧品牌色竖条，详情面板标题上移、面板跟随滚动
- 配图页：「用途」与「平台」两个选择器合并为一行双列节省空间，字段标签加粗加大提升可读性，风格选择器从全圆改为圆角矩形
- 统计卡片按下态加品牌色描边环，搜索栏输入框去掉内边框与外壳融为一体

## 1.3.72 (2026-09-12)

- 发布中心不再把同一批发布记录印两遍，而且改掉了其中自相矛盾的那处：看板「已发布」这一列此前对每条都写死「已回流数据」，而同一页下方对同一条却写「待回流」。现在三态（缺作品链接 / 待回流 / 已回流）只算一次、只显示一处
- 发布中心找回「逐条推进」：存草稿箱 / 贴作品链接 / 标记已发布这些操作此前只有在刚建完计划那一刻按得到，关掉或刷新就再也找不到，存量计划推不动
- 机器人回执里的「发布计划」链接点进来能看到那一条了：此前那个地址带的计划编号页面根本不收，点进去只看到最近 8 条，自己那条常常不在里面
- 浮标助手对「发出去」这一页的介绍跟着改对（原来还写着已经并进看板的那张卡）

## 1.3.71 (2026-09-12)

- 创作工坊找回「给正文配图」入口：这个功能的后端一直是好的，但它在工坊里唯一的挂载点是一段从没被渲染过的代码，而「出图工位」页至今写着「创作工坊 · 正文配图」指引用户去找。现在它在「标题封面」页签下
- 创作工坊打开更快、更轻：草稿列表不再把每篇稿子的每一版正文都拉到浏览器（只取列表要用的字段 + 版本数），选中那一篇的正文单独取最近 10 版；选中稿之后的四段取数并成两段
- 全网热榜页每个平台只下发前 60 条（原先把库里全部 1843 条塞进页面，而首屏每个平台只显示 10 条）。页签上的条数仍是库里的真实条数，展开时会说明「已显示前 60 条 · 库里共 N 条」

## 1.3.70 (2026-09-12)

- 生产数据库连接池从 3 提到 15：此前一页里所有并发查询都被池子排成「三条一批」，跨区往返 32ms 一批，代码怎么并发都没用。线上中位耗时：看效果页 1016→604ms、首页 682→580ms、技能页 1507→1244ms（此项为服务器配置，整机版/私有化请按 .env.production.example 里新的说明自行设置）
- 登录态解析从三次查库并成一次：它挂在**每一个**已登录请求上，此前「会话 → 工作区 → 当前账号」首尾相接，跨区一跳 32ms，常态白等 96ms、切号落空时最坏 160ms
- 内置智能体落库从 13 次串行改为一次并发：13 个内置模板（3 条流水线 + 3 条接力 + 7 个职能 bot）此前逐条写库，而「技能·连接器」「任务记录」「AI 的 list_agents」「机器人路由」每次读都要跑一遍，一次约 416ms
- 挑选题 / 做内容 / 看情报 / 技能·连接器 / 数字员工五页的取数改为并发：原本各有 4~7 段互不依赖却排成串行的查询
- 挑选题页的「推荐从哪来」冷启动卡改为流式：它自己要查十来次库，而对已经上手的账号根本不显示——不该让每个人先等它
- 竞对监控页去掉一处算完从不显示的统计
- 「点了终止，后面的工具不许再跑」这条守卫此前本机 5 次挂 4 次（用例自身的竞态，产品侧闸门一直是对的），已改成确定性用例

## 1.3.69 (2026-09-12)

- 「技能 · 连接器」页少等五次往返：账号、可选模型、定时用的渠道名、建议定时、职能 bot 台账改为一次并发取（其中渠道名原先是循环里逐个查）。这是 1.3.68 上线后在生产上量出来的最慢一页

## 1.3.68 (2026-09-12)

- 系统设置页不再白屏五秒：数据源探活从「挡着整页等」改成页面先出来、探测结果随后补进来，探测结果 60 秒内复用；探测中显示占位行而不是空白
- 首页少查七次库：删掉一批取回来从不显示的数据（其中一项自己还要再查十来次），待办清单只取要用的字段并封顶 200 条，「几条未完成」改用真实总数
- 竞对监控页只取统计要的四个字段，榜单上的 50 条才取整行，订阅账号多、作品多时不再把整库作品读进内存；页面自动退回「全部平台」时会说明原因
- 竞对页找回两个入口：插件一键采集、公众号文章导入（1.3.62 改版时随旧版式一起消失，功能一直在）
- 手机上首页派活框的按钮不再被挤成竖排一个字一行
- 浮标助手的样式从每页内联改为进全站样式表，每个页面的 HTML 少约 11KB
- 清理：删掉九个没有任何界面在用的服务端动作、两个改版后再没人引用的组件，以及全仓未使用的 import

## 1.3.67 (2026-09-11)

- 全站视觉体系升级：面板渐变边框回归（CSS 变量化）、页签键盘导航、运维后台布局精调、各页面组件统一语义化标签

## 1.3.66 (2026-09-11)

- 版本升级：整机版更新包内容刷新

## 1.3.65 (2026-09-11)

- 知识范围修正：把员工最后一条资料也禁用后不再退回读整库，而是什么都不读；绑定唯一键补上工作区，两个工作区给同一个内置员工绑同一份资料不再互相覆盖；记忆/标签/素材类型的取值加校验
- 内容工单加固：状态变更与动态事件同一个事务，带期望阶段抢占（两人同时验收和驳回只有一个成功）；派活关系改关系表，并发派活不丢；挂选题/发布记录会校验归属
- 按任务选模型改为严格：指定的渠道被删、失效或境外受限时直接报错，不再静默换成别的渠道；健康收件箱会提前列出失效的指定渠道
- 固定角色接力改为持久等待：派出子运行后流水线把等待写进库，子运行结束由终态钩子叫醒续跑，部署重启不丢；巡检兜底漏叫醒与超时（超时会终止子运行）
- 员工档案与健康页的成功率、Mock 比例改用全量计数，不再被取样截断；单次定时失败不再把员工标成故障；引用不再混进执行步骤
- 能力注册表里每个生成技能显示自己的用量，不再套用 run_skill 总量；手动跳过定时不再写进错误栏
- 绩效口径对齐：时效 = 工单建单到验收；被接受率按工单去重；单位成本只算验收工单所挂运行的成本
- 需要跑一条 SQL（prisma/postgres/58-audit-hardening.sql），并重跑 02-rls.sql

## 1.3.64 (2026-09-11)

- 数字员工知识范围：在员工档案里绑定它可读的资讯条目/标签、素材类型、人设记忆；有绑定的员工只读范围内的资料，范围外直说不在范围；每次执行的引用可回溯到源对象
- 内容工单：任务记录页新增「内容工单」看板，串起 选题 → 起稿 → 审校 → 待发布 → 已发布 → 复盘；验收/驳回（必须写原因）/返工计数；工单只关联草稿、运行、发布，不复制正文
- 能力注册表：「能力」页汇总动作工具、自写工具、生成技能、做法技能、执行器、消息渠道、模型渠道，标出「装了但不可用」并给修复入口
- 员工健康与异常收件箱：模型/渠道/执行器/知识/自写工具/定时六类只读诊断，按影响的员工聚合，可导出脱敏诊断包
- 固定角色接力：三条内置接力模板（情报→选题、选题→写稿→审校、数据→复盘→下周选题），每跳定义交付物与预算，上一跳没交付下一跳不启动，发布不进接力
- 可解释绩效 v1：员工档案显示可靠性、时效、被接受产物率、返工率、单位交付成本五项及样本量，样本不足写「不足」，不做总分
- 新增内置技能「提词稿 + 分镜」：把正文变成可直接对着念的分页提词稿与逐镜分镜表（画布录制留在桌面端，本版未做）
- 按任务选模型：首页派活框选的模型跟着这次执行走；一键任务卡可指定模型渠道；定时任务可单独指定（覆盖卡上的），流水线每一步和接力的每一跳都走同一条渠道；执行过程页显示指定渠道
- 需要跑三条 SQL（prisma/postgres/55-agent-knowledge-binding.sql、56-content-work-item.sql、57-provider-choice.sql），并重跑 02-rls.sql

## 1.3.63 (2026-09-11)

- 任务记录与工作流页去掉写死的示例数据：没有真实运行/定时就显示真实空状态，耗时只按真实起止时间算，去掉只弹提示不做事的按钮
- 新增数字员工档案（/workflows?agent=ID）：状态（可用/忙碌/受阻/故障/停用）、正在跑什么、近 7/30 天成功率、近 30 天花费（含落 Mock 次数）、工具/步骤、定时、一键任务、渠道、最近产物与错误
- 定时任务运营中心：按规则现算未来 7 天实例，标出错过 / 会被上限拦下 / 撞车，给出最多调用次数上界；今天的可以补跑、跳过、停用
- 每次 AI 执行显示经济账：真实调用次数、token、估算美元、预算用了几成、工具调用次数、落 Mock / 供应商兜底次数、钱走平台还是自带 Key；并可一键跳到执行它的智能体档案
- 定时列表里指向一键任务的计划不再误显示为「已经被删了」

## 1.3.62 (2026-09-10)

- 竞对页拆出 CompetitorConsole 组件，布局精简
- 素材库页拆出 LibraryConsole 组件，去掉冗余包裹层
- 发布面板视觉优化

## 1.3.61 (2026-09-10)

- 创作工坊改三栏工作台：左侧草稿列表 + 中间编辑器 + 右侧 AI 助手面板，右栏可折叠
- 工作流页改双面板布局，标题改「技能 · 连接器」
- 热榜页加平台标签切换
- 智能层级卡片加图标与层级标签（L1–L4）
- 全站视觉精细调整：间距、圆角、卡片密度统一
- 「最近更新」卡从运行设置移到使用帮助页

## 1.3.60 (2026-09-10)

- 热榜分析安全加固：签名采纳令牌、LLM 输出归一化、并发去重
- 合规检测器：平台/文案变更作废旧报告，改写请求序号防过时响应
- 文案漂移治理：平台数、更新间隔、版本号全部改为常量派生
- 发布记录改三态显示（缺链接/待回流/已回流）
- 测试基础设施修复：fire-and-forget 追踪、schema push 重试

## 1.3.59 (2026-09-10)

- **偏好回路**：每次 AI 执行开跑前，自动把最近 30 天的负向信号（工具被拒绝 ≥2 次、失败率 ≥50%、被打回过什么）算成几句教训带进系统提示，后续执行不再犯同样的错。统计窗口即过期机制，不进记忆表
- **技能建议提前到跑完当场**：原来每天凌晨才算一次「这件事你做过三次了」，现在执行完当场就提醒，通知直接链到那次执行的页面，页上就有存技能按钮
- **AI 自写工具（整机版/私有化版）**：AI 用现有内置工具拼一个新工具（一段 JS 跑在沙箱里），起草自动、启用靠人——技能中心看过代码点启用才生效。会不会改数据、花不花钱由声明的内置工具推出，不由模型自报。SaaS 上恒关
- **执行器误判修正**：真调过工具后收尾提到「示例」不再被当成编数据打回
- **热点结合分析更稳**：模型返回的结构不合规范时不再让页面崩溃，会自动换成可用的兜底结果并标注；快速连续分析两个热点时，只显示最后一次的结果
- **一键采纳修正**：采纳按钮在请求中即禁用，并发重复点击最终只留一条选题；采纳内容改为凭服务端签发的分析凭证落库，客户端无法伪造热点、切入角或契合度评分
- **发布记录状态更准**：已关联作品但互动数据还没回来的记录显示「待回流」而不是「已回流」；「缺链接的记录」按账号全量统计
- **合规检测报告与文案对齐**：改动文案或切换平台后旧报告自动作废，避免右侧报告与左侧内容不一致
- **热榜页修正**：「最近更新」改取全站最新采集时间；页面 SEO 描述、结构化数据、站点关键词与页头提示里的平台数、更新频率、版本号全部改为从实际配置读取（此前写「60 秒更新」，实际每 30 分钟）
- **帮助页死链修复**：「去看基因」指向合并后的「看效果」页，旧地址 /genes 自动跳转

## 1.3.58 (2026-09-09)

- **对话里能直接管自己的账号**：新增查账号 / 添加账号 / 修改账号三个 AI 工具。说「把我的 X 账号加进来，handle 是 xxx」就直接加好，注册时的占位账号会就地升级，重复说不会建重，之后可以接着派采集
- **做不到的事会记成能力缺口，不再只说抱歉**：AI 遇到没有工具的请求，会先看现有工具能不能拼出来，不行就把缺口记下来（想做什么、缺哪一步、建议什么工具、页面上现在怎么手动做）。执行过程页上能一键复制成开发提示；平台运维的 AI 页汇总所有缺口。AI 若只回「无法执行」会被打回一次要求记缺口
- **首页派活框不再被输入法误触**：中文输入法组字、或打英文按回车上屏时，不会再把半截话派成任务。浮标助手与选题拒绝原因框同样修正
- **派出去的任务可以当场终止**：首页「已经开始」横幅和「正在办的事」列表里每条 AI 执行都有「终止」

## 1.3.57 (2026-09-09)

- **写稿页标题区收成一行**：复制发布包/正文、导出 Word/演示文稿/图文卡收进「导出 ▾」下拉，与一键发布、登记发布并排；标题区不再单独占一排工具条
- **目标平台与「保存修改」上移**：挪到编辑框上方「正文内容」那一行，紧挨字数与暂存状态；编辑框下方只剩改写、教练调优、去 AI 味
- 版本时间线不再重复显示正文预览，只留版本、作者、时间与差异摘要

## 1.3.56 (2026-09-06)

- **「问 AI」并进首页那一框**：同一句话，「开始执行」就派活（会动数据、花额度），「先问问」就对话（只答不动手，⌘/Ctrl+Enter），答案直接出现在框下面；答完像是要它去做的，再给一个「让它直接去做」按钮，按了才真跑。模型选择与参考图也在这一框。侧栏不再有「问 AI」，/assistant 只剩「执行过程」（看某一次执行、确认、追问）；首页不再浮那个对话球
- **「最近更新」搬进「运行设置」**：直接读 CHANGELOG.md 列最近 5 个版本，首页不再占一块
- 首页：试用进度卡与「AI 还不认识你」引导卡挪到「本周作战」标题之前，标题不再和报告正文被拆开

## 1.3.55 (2026-09-06)

- **全站视觉升级**：账号切换器从原生 select 升级为带平台色标的自定义下拉浮层；开场向导全面重做（平台卡片带色标、步骤进度条、链接实时解析、表单校验更友好）；账号管理表单改结构化卡片布局；语言切换器改精致胶囊按钮；模型选择器样式统一；对话输入框聚焦发光+参考图内嵌预览；快捷卡片带彩色图标与悬停动画
- **英文界面补齐**：模型选择器标签与说明（自动/自接入/平台默认）、「我的账号」占位名在英文下显示 My Account（顶栏/作战报告/AI 助手/对话页统一）
- 客户端 1.2.18 签名文件更新

## 1.3.54 (2026-09-06)

- **开场向导真的会建「自己的账号」了**：贴了主页链接，注册时那条「我的账号 · 多平台」占位行就变成「X @你的ID」这样的真账号（顶栏与账号管理一眼能看出是你的号）；占位行已被别的号占着就另建一条并切过去；重跑向导不会多建。没贴链接时，只选了一个主战平台就把平台落到账号上，并明说「还没记下主页」；链接认不出时列出认得的链接形态，不再让人以为记上了

## 1.3.53 (2026-09-06)

- **派活只剩一个框**：首页「今天」的框就地开跑（授权范围也在这一框下面改），不再跳到「新任务」再点一次；「新任务」改名「问 AI」，只做对话问答和看某一次执行的过程与追问，不再有第二个一模一样的输入框。右下角助手的「让它直接去做」和老链接 /assistant?goal= 都落回首页预填，仍然是你按了才跑

## 1.3.52 (2026-09-06)

- **有了真正的公开首页**：没登录打开站点看到的是「先知道做什么，再谈怎么写」的产品介绍（八条选题来源、价格、下载入口、我们坚决没做的事），不再是一个要手机号的登录框。登录页、总览页、插件商店描述、分享卡片统一成同一句话
- **下载页、插件页、价格页免登录可看**：看一眼有没有 Windows 版、价格多少，不用先注册。新增公开的「今日选题榜」（/topics-today）：跨平台正在扩散的话题 + 按赛道的常青题 + 三十天内的节点，免登录可看、可被搜索引擎索引
- **桌面客户端重新定位为「数据回流器」**：下载页第一屏讲的是「装上它，同行动态与 X / TikTok 主页数据每天自动回来，不用装插件」，独立窗口 / 托盘退到第二屏。抖音 / 小红书 / B 站创作者后台的数字仍需插件，页面如实写明
- **十分钟开场向导（/onboarding）**：首次注册直接进入；填赛道与主战平台、贴一个主页链接、贴或选几个同行，系统建人设、订同行、派采集、当场跑一次推荐，把八个选题来源逐个点亮。首页人设空白的引导卡也改指向它
- **邀请一位创作者，双方各得 7 天标准版**：套餐页有你的邀请链接；免费档拿到新的 7 天试用，试用与付费档到期日顺延，永久买断不叠加；每月最多 20 次，每笔都有记录
- **游客 → 注册接力**：演示模式里点「注册 / 登录」会带着当前这一页过去，登完原地接着看，不再落回首页
- **本周作战报告可以下载成分享图**（3:4，小红书 / 朋友圈比例），数字与报告一致，拿不到的指标印「—」
- **公众号竞对查询改为平台代付、按套餐限次**（试用 30 / 标准版 60 / 自带 Key 版 20 次每月），平台配了数据源 Key 之后个人创作者不必再自备；用完如实提示并指向导入
- **网站有了漏斗埋点**：看首页 → 点体验 → 发验证码 → 注册 → 建人设 → 首次推荐 → 点下载 → 首次数据回流，运维台新增「增长漏斗」页；只记事件不记 IP / UA
- 手机上：「添加到主屏幕」后长按图标直达看选题 / 存灵感 / 看数据；首页如实写明采集与发布仍走客户端或插件，暂无手机 App
- 删掉 public/ 下一份过期的静态 robots.txt / sitemap.xml（它对所有爬虫 Allow: /，与真正生效的动态 robots 自相矛盾，且让本地 /robots.txt 报 500）；现在只有 app/robots.ts 与 app/sitemap.ts 这一份
- 需要跑一条 SQL（prisma/postgres/53-growth-funnel-referral.sql）

## 1.3.51 (2026-09-05)

- 新渠道：**企微智能机器人（长连接）**。在企微管理后台「应用管理 → 智能机器人 → API 模式 → 长连接」拿 BotID 与 Secret，填进「消息渠道」即可，不需要公网回调地址、不需要域名。群里 @它或私聊它对话、派任务；派出去的任务它会把回复留着最多 10 分钟，跑完把结果接在后面。只答不推（没有定时推送）。每个机器人只能有一条连接，别同时接在别的工具上

## 1.3.50 (2026-09-05)

- 职能机器人的台账在卡上看得见、改得了：它在盯什么、做到哪、已报过几条，可以删单条、记入新的、清空已见清单（清空后下次全当新的再报）
- 首次派带台账的 bot（情报员/选题官/复盘官）时，它会先问你要盯哪些号或话题，记进台账再动手；定时自动跑到时没人回答，就按现有监控先干并在汇报末尾提醒你来设盯单
- 群里用 /智能体 绑到带建议定时的 bot 时会提一句有哪些定时可开（默认关，只能在登录后的智能体页开，群里一句话开不了）
- 执行模式改用 MiniMax M2.5 推理模型（此前 Text-01 会编数据、零工具调用；同一家、境内）
- 桌面客户端 1.2.18：采集令牌标签带机器名（两台电脑分得开）；采集浏览器的 profile 命名为「烽火台采集浏览器」，跟日常 Chrome 一眼分清；Windows 等登录时也把窗口抬到前台

## 1.3.49 (2026-09-05)

- 职能机器人补齐三块（参照 Grok Bot 市场的 bot 形态）：
  - **台账**：每个 bot 有自己的工作状态（盯单、上次做到哪、已见清单），只有它自己看得见。情报员和选题官汇报前先过一遍已见清单，同一条帖子或热榜不再天天重报；复盘官记住上次复盘到哪，不重复上次说过的话
  - **技能带「什么时候用」**：写手自带公众号排版、小红书排版、抖音口播、视频号脚本、知乎长文五条技能，每条写明用户怎么开口时用；没装的技能会标「未装」而不是假装跑了
  - **建议定时**：情报员（每天 8 点盯竞对与热榜）、选题官（每天 9 点给三条选题）、复盘官（每周一复盘上周）装上后会问一次要不要开，默认关；开了才建一键任务卡与定时，可在定时任务里改或停
- 导出/导入修正：此前导出一个职能机器人会丢掉它的白名单与人设，到别处装上是个空流水线；现在整份带走，老格式的文件照旧能导入
- 定时现在也能直接指向一键任务卡（此前只有消费端认这种定时，没有任何入口能建）
- 需要跑一条 SQL（prisma/postgres/52-agent-ledger.sql）

## 1.3.48 (2026-09-05)

- 审计中危批（16 条）：采集任务被取代/取消后，执行器晚交回的结果不再复活它、也不再写进数据看板；执行器领走却一直不交回的活试满 3 次判失败，不再每 15 分钟重开同一页
- 客户端交活会看服务端回应：令牌被吊销时自动解除登记并让「允许」按钮重新出现；交活失败如实显示，不再默默记成成功（客户端要更新到 1.2.17）
- 通知与提示：不再把桌面客户端叫成"插件"；"常见原因是没登录"只在错误确实与登录有关时出现；托盘项名称与实际一致
- 数据：采集的逻辑日按北京时间（此前容器 UTC 会把凌晨采集写进前一天）；已存在的竞对作品会回填空的标题/发布时间/链接；热度按合并后的播放量算；定时抓取通道合并而非覆盖插件采到的字段；接口没给的指标不再填 0；小红书空卡片不入库
- 整机版「开启浏览器操作」改用独立采集 profile，不再要求退出日常 Chrome；托盘「打开采集浏览器」不再卡住窗口；读网页读回空正文不再算成功

## 1.3.47 (2026-09-05)

- **新增 7 个按职能分的机器人**：情报员（采集/监控）、选题官、写手、出图师、发布官、复盘官、合规官。每个都有自己的工具边界——情报员只读不写、发布官建计划必停下来等你点头、出图师不写稿。可以在群里「换成 情报员」让它出面，也可以在渠道设置里把某个群固定绑给某个职能，AI 助手也会按职责自动挑
- 群机器人绑定了职能 bot 之后，它**真的只能用自己那摊工具**（此前绑定只影响说法，不影响权限）
- 已经在用的工作区会自动拿到这批新机器人（你主动卸过的不会复活）

## 1.3.46 (2026-09-05)

- **首页「今天」的框打完字直接开跑**：不再跳到「新任务」页让你再点一次「开始执行」。做法是把这句话放进一个只有你自己的登录会话能写的一次性槽，助手页读到就自动开跑、用完即清——外部链接仍然只能预填、不能触发执行（那条防线没动）
- 桌面客户端 Mac 1.2.16 一键更新已发布（Windows 待构建）

## 1.3.45 (2026-09-05)

- **检测不到插件就立刻用浏览器**：判断"谁来采"只看最近 25 分钟内真的来领过活的执行器——半个月前装过、早已卸载的插件不再被当成在线，不会再把活派给一个永远不会来领的东西，回执也不再说"插件/桌面客户端都在"
- **派下去立刻开始**：桌面客户端领活间隔从 60 秒缩到 20 秒，且网页派完活的瞬间就叫客户端领一次，几秒内开始采（客户端要更新到 1.2.16）；执行过程页在桌面端每 8 秒刷新一次状态
- 执行中状态徽标由"等浏览器插件"改为"等采集执行器"

## 1.3.44 (2026-09-04)

- 等你登录时不再堆标签页：之前每次任务都会新开一个登录页留着（真机上堆到 5 个），现在始终复用同一页——你在哪一页登，它下次就接着用哪一页（客户端要更新到 1.2.15）

## 1.3.43 (2026-09-04)

- **小红书这类「整页跳到登录页」的站点现在能正确识别了**：此前判断登录墙时页面还没跳转完，于是判不出来，最后报「这个号可能还没发过内容」。现在会等地址稳定再判（客户端要更新到 1.2.14）
- **认得出「盖在内容上的登录弹层」**：小红书的登录墙既没有密码框、也没有登录链接，是一个弹层——此前两种判据都漏掉。现在按「扫码登录/手机号登录/新用户注册」这类只在登录界面出现的字样识别，正常页面不会误判
- 采到 0 条时的说明改为先指向最可能的原因（采集浏览器没登录该平台），不再把「这个号还没发过内容」当唯一解释

## 1.3.42 (2026-09-04)

- **让你去登录时，那一页原样留着**：之前会一边说「窗口停在这一页请登录」，一边把它导航回空白页——你看到的是一个空白页和一句让你登录的话。现在要你登录的页面不会被清掉，采集器下次也会另开一页，绝不抢它（客户端要更新到 1.2.13）
- **不管哪种「需要登录」都停下来等你**：此前只有跳登录页/有密码框这类明显情况会等，而小红书这种「页面能打开但只有登录入口」的情况是直接判失败。现在两种都等你登完，登完自动接着采，不用再派一次

## 1.3.41 (2026-09-04)

- **采集浏览器不再抢你正在用的标签页**：之前它会复用"任意一个空白页"，而你为了登录平台正是在那唯一的标签上打开站点——于是你打开的页被导航走、刷新后回到空白页，登录永远做不完。现在它只认自己上次留下的那一页，你在上面开了别的东西就另开一页（客户端要更新到 1.2.12）
- **驱动浏览器时页面上有明显标识**：橙色边框 + 右上角"烽火台 · 正在采集这一页（只读）"，撞上登录墙时变成"请在这一页登录，登完它会自己接着采"。标识不挡任何操作，采完自动消失
- **没有插件时不再让你去开浏览器**：AI 的上下文里现在写明真实的执行器状态（桌面客户端每分钟领一次活 / 插件要等你下次开浏览器 / 两者都没有就如实说派不出去），不会再出现"请确保浏览器已打开并连接插件"这种与你实际情况不符的说法
- AI 不再把工具名（如 `list_browser_tasks`）当成命令写给你敲

## 1.3.40 (2026-09-04)

- **修复「等你登录」根本不工作**：撞上登录墙时，采集浏览器弹出的是另一个新标签，而程序盯的是原来那个永不刷新的标签——你登完了它也检测不到，一直等到超时并告诉你「等了 5 分钟还没登上」。现在推到你面前的就是它正在判的那一页，每轮重新加载再判（客户端要更新到 1.2.11）
- 同一个洞的第二半：单个任务被 2 分钟硬超时罩着，而等你登录要等 4 分多钟，于是等待总被腰斩、并报一个与真实原因无关的「超过两分钟没跑完」。超时改为按等待预算推算
- 网页打不开（网络/DNS/代理不通）时如实说打不开，不再报成「站点改版了，解析器取不到内容」
- 公众号/微博/快手/知乎/头条/百家号/视频号这些没有可直接打开的公开主页的平台，派竞对采集时当场说清并指出该走数据源或手动导入，不再派一个注定失败的任务、最后告诉你「常见原因是没登录」
- 对外 API 派采集任务时，若命中「刚采过就复用」的冷却，如实回 `status: done` 并说明是复用上次结果，不再回「已排队」让调用方把旧数据当成新采的
- 桌面端两处授权说明改正：采集用的是独立的采集浏览器，**你不用退出日常 Chrome**，代价是每个平台各登录一次

## 1.3.39 (2026-09-04)

- 修复：刚采完之后再派同一个任务时，队列里卡着的同一个旧任务没有被清掉，晚点会被领走再采一遍

## 1.3.38 (2026-09-04)

- 「同一个采集任务」的判断改为按账号：回填自己主页的任务，同一账号即视为同一个活（此前只逐字比对参数字符串，键顺序不同就认不出来）

## 1.3.37 (2026-09-04)

- 重复派同一个采集任务时，以新的为准：之前排着或正在跑的同一任务自动取消，等它的 AI 执行改等新任务，不会再出现「两条一样的任务各跑一遍」

## 1.3.36 (2026-09-04)

- 采集不再反复打开、关闭浏览器：采集浏览器只用一个固定标签页，采完停在空白页等下次，窗口一直在（客户端要更新到 1.2.10）
- 同一个主页半小时内不重复采：刚采完再派，直接复用上次结果；已被客户端领走的任务再派不会排第二条
- 采集失败后退避再试（第 1 次 10 分钟、第 2 次 30 分钟），不再一分钟一次地反复打开同一页——对平台来说那正是最像脚本的行为
- 执行中的状态由「等浏览器插件」改为「等采集执行器」

## 1.3.35 (2026-09-04)

- X 采集读不到内容时，提示先指向最常见的原因：采集浏览器还没登录 X（未登录时 X 给的是精简页面）。真机验证：在采集浏览器里登录一次后，桌面客户端已能完整采回自己的 X 主页（作品、发布时间、粉丝）

## 1.3.34 (2026-09-04)

- 采集碰到需要登录时**停下来等你登**（最多 5 分钟）：采集浏览器的窗口会摆到你面前停在登录页，你登完它自己接着采，不用再派一次
- 执行器交回的内部错误码在服务端翻成人话，旧版客户端也立刻受益（不用重装）

## 1.3.33 (2026-09-04)

- 修复 X（推特）采集一直返回 0 条：X 移除了页面上全部 data-testid 等定位属性，解析器改为退回 article 元素识别推文
- X 改版导致读不出内容时，如实说「这个平台改版了，解析器暂时取不到内容」，不再说成「这个号可能还没发过内容」；**取不到内容的作品一条都不入库**（避免空记录污染数据看板与基线）

## 1.3.32 (2026-09-04)

- **二级弹窗与展开辅助卡片双语深度优化**：
  - 选题就绪与协同（SourceReadinessCard / TopicVotes / TopicRescore / TopicActions）：来源解锁清单、团队意见表决条、AI 重新评分交互与采纳/拒绝偏好反馈面板全量支持中英双语切换。
  - 创作工坊与多平台派生（SkillPanel / DeriveCard / DraftList）：技能参数配置与运行结果卡、富文本排版复制、跨平台一键派生与同源稿对比、草稿快速检索与版本筛选全量双语化。
  - 智囊团会诊与弱项联动（AdvisorPanel / WeakDimConvene / VerdictButtons）：12位受众与专家混编会诊卡、六维体检专项会诊直达、提案采纳/改判交互完整双语化。
  - 渠道对话与合规反馈抽屉（BotChatsDialog / FeedbackPanel）：群聊/私聊/用户详情抽屉、飞书群同步与合规误报复核面板全面双语支持。
- **全局智能助手与账单出图等核心模块双语本地化全面收官**：
  - 侧边栏/全局智能助手（GlobalAIAssistant）：快捷派活卡片、快捷操作气泡、会话清理确认、派发状态提示完整支持中英双语切换。
  - 账单与订阅中心（Billing & Checkout）：定价方案对比卡、按月/按年与买断方案切换、月度产出台账、AI Token/函数消耗账单、订单明细与状态徽标、扫码支付弹窗及退款流程全面双语化。
  - 对标账号高热作品榜单（CompetitorTopPosts）：TOP 3 领奖台卡片、平台指标筛选（播放/点赞/评论/收藏/转发/硬币/弹幕）、AI 爆款拆解维度分析与紧凑表格视图全面双语化。
  - 封面工位（CoverStation）：文案提取、生成结果标识与合规风险提示、草稿历史封面列表、字体倾向与画面装饰设置、内置/自定义风格选择与新建风格面板全面双语化。
  - 账号与数据中心（AccountManager / Data Page）：授权引导、平台绑定提示、数据全景看板与图表全量双语无缝切换。

- 采集浏览器还没登录某个平台时，如实说「采集浏览器还没登录这个平台」并把那个窗口摆到你面前停在该页，不再报一句「可能没加载完，或这个号还没发过内容」
- 修复：页面刚打开时执行上下文还没建好，采集会以一个与真实原因无关的报错失败（客户端要更新到 1.2.8）
## 1.3.31 (2026-09-04)

- **全站深度双语本地化与零残留优化**：全面清查并修复 AI 助手、创作工坊、数据看板、人设中心等高频界面的中英双语适配。包括快捷派活卡片、正文编辑器诊断徽标、版本演进时间线、发布与导出弹层、数据点亮进度条、核心指标安全线以及冷启动人设生成流程，实现无缝双语切换。

## 1.3.30 (2026-09-04)

- 桌面客户端改用**采集专用浏览器**：不再要求你退出日常的 Chrome。它用自己独立的数据目录，跟你日常的 Chrome 各跑各的，采集任务在后台自动把它拉起来
- 为什么必须这样：Chrome 从 136 版起拒绝在你默认的用户数据目录上开调试端口，「复用你已登录的日常 Chrome」这条路在新版 Chrome 上已经走不通
- 每个平台**只需登录一次**：登录态长期留在采集浏览器里，升级客户端不会清除。首次采某平台时它会把窗口摆到你面前停在登录页，登完之后几个月都不用再管
- 托盘菜单新增「打开采集浏览器（登录用）」与「清除采集浏览器登录数据」
- 修复：服务端镜像里没有带上插件解析器文件，导致桌面执行器取不到采集脚本（客户端要更新到 1.2.7）

## 1.3.29 (2026-09-04)

- **公众号「读我自己后台的数据」恢复**：授权一次之后，插件可以打开你**自己的**公众号后台，读你**自己**作品的阅读/在看/完读率回填到数据看板——与视频号/抖音/小红书/B站四个创作者后台同一条通道、同一套约束（只读已渲染的页面、不点击、不发布、不调用平台接口、不碰 Cookie、不代替登录）
- 与那四个不同的两点：① `mp.weixin.qq.com` **不在插件的安装权限里**，要用这一项得在插件设置页**单独点一次授权**（浏览器弹确认，随时可在扩展页撤销），没授权则一行都不注入；② 它是**官方发行版专有的可选模块**，开源发行版不含它——没有它时设置页那一块整个不显示，而不是留一个永远不会发生任何事的开关
- 设置页新增「每天自动回填我的后台数据」开关、执行时间、站点授权与立即试跑，最近一次结果就写在下面那一行（系统通知可能被系统静默丢掉，得有个查得到的地方）
- **公众号「竞对」采集保持删除，且不会恢复**：那条是拿你的登录态去查**别人**的号，风险落在你自己的公众号账号上
- 隐私政策与服务条款同步更新（LEGAL_VERSION → 2026.09.5）

- 派活回执叫对名字：登记了桌面客户端的工作区，AI 会说「已排给你的桌面客户端（每分钟领一次活）」，不再把客户端叫成「插件」；两者都有时写「插件/桌面客户端」
- 执行中「等结果」那行说明同步改成按执行器区分（客户端每分钟领一次；插件等它下次醒来）
## 1.3.28 (2026-09-04)

- 桌面客户端连云端账号时，页面顶部会出现「允许这台客户端操作浏览器采集？」——点「允许」即完成登记，AI 的采集任务从此由客户端用你自己的 Chrome 后台跑完，不装插件也能采（此前要到折叠的设置组里找那张登记卡）；执行器报错（如 Chrome 开着没带调试端口）会直接显示在同一位置
- AI 执行反问「走插件还是客户端」的又一种写法（「如果您希望…请告诉我」）也会被认出来打回
- 业务页面全量双语化收尾：挑选题、对标账号、看数据、创作台、内容库、素材、合规、成员、账单、出图、发布、人设、技能与热榜页的正文、按钮与状态词全部支持中英切换（此前只有导航与部分卡片双语）

## 1.3.27 (2026-09-03)

- **移除公众号采集**：插件不再进入你自己的公众号后台，也不再匹配 `mp.weixin.qq.com` 下的任何页面。删掉的是两件事——「公众号竞对采集」（在你后台里调 searchbiz/appmsgpublish 拉对标号的文章列表）与「每天自动回填我的公众号数据」
- 为什么删：这两个接口不是微信官方开放的数据接口，自动化调用可能违反《微信公众平台服务协议》，而被限接口、被封功能的是**你自己的公众号账号**，我们无法代为申诉。默认关 + 风险确认 + 保守节流只能降低概率、不能消除风险，所以整条撤掉
- 替代路径：公众号竞对文章改由服务端商业数据源取（配 `BEACON_NEWRANK_KEY`），或用 wechat-article-exporter 在本地导出 JSON 再从「对标账号 → 公众号文章导入」导进来；公众号**发布**不受影响（走官方接口）
- 自有公众号后台数据暂时没有自动回填通道；视频号/抖音/小红书/B站 四个创作者后台不受影响
- 服务端不再把公众号/视频号的竞对采集派给插件：插件采不了它们，派下去只会交回「更新 0/0」这种看起来成功的结果
- 隐私政策与服务条款同步删掉这两段披露（LEGAL_VERSION 升到 2026.09.4），插件设置页的「公众号竞对采集」风险开关与「每天自动回填我的公众号数据」开关一并移除

## 1.3.26 (2026-09-03)

- AI 执行一个工具都没调却宣称「采集已完成」并给出「示例数据」、或只说「请稍等」就收工、或反问你「走插件还是客户端」——这三种回答会被认出来打回让它真的去做；叫两次还这样就如实判失败，不再显示「已完成」，编的数字也不会入库
- 桌面客户端 Mac 1.2.6 一键更新清单已发布；Windows 1.2.6 待构建后补入

## 1.3.25 (2026-09-03)

- 桌面客户端（Mac/Win）连云端账号也能「浏览器操作」：在「采集助手」页把这台客户端登记为采集执行器，AI 派的采集任务由客户端在后台用你自己的 Chrome 跑完交回，不装插件也行（客户端要更新到 1.2.6）
- 插件/执行器领活时自报会做哪些任务，服务端只派它会做的：旧版插件不会再领到自己不认识的活然后反复失败；派不出去时如实说要更新插件或登记客户端
- 私有化 Postgres 需执行 prisma/postgres/51-ingest-token-kinds.sql；整机版 SQLite 自动同步
- 首页与情报板块深度多语言化：Today 任务派发卡、本周作战、人设引导横幅、试用节奏与数据指标卡全面适配英文
- 全站二级阶段标签栏（PageTabs、IntelTabs、PickTabs、StudioTabs、EffectTabs、AssetTabs）全面双语化
- 热点聚合中心（hotlists）各平台榜单、热点聚类分析与热度指标全量支持中英切换

## 1.3.24 (2026-09-03)

- 本机浏览器改为**优先**：整机版/桌面端开了「浏览器操作」且 Chrome 带调试端口跑着时，AI 的采集任务（采竞对主页、回填自己的 X/TikTok 主页、读网页）当场用它跑完直接给结果，不再排给插件等它醒；没开或 Chrome 没在跑时照旧排插件，回执里说清怎么把它叫起来
- 「设置 → 本机权限」的浏览器操作变成一个开关：点「开启」服务端自己把你的 Chrome 带调试端口拉起来（用你的默认 profile，登录态都在），不用再去托盘启动、回来手填端点；Chrome 正开着没端口时如实提示先退出，我们不替你关
- AI 把工具调用写成 JSON 块给你看（「您可以通过以下命令查看进度」）这种回答会被认出来并叫它重发，不再当成答完了
- 首页与情报板块深度多语言化：Today 任务派发卡、本周作战、人设引导横幅、试用节奏与数据指标卡全面适配英文
- 全站二级阶段标签栏（PageTabs、IntelTabs、PickTabs、StudioTabs、EffectTabs、AssetTabs）全面双语化
- 热点聚合中心（hotlists）各平台榜单、热点聚类分析与热度指标全量支持中英切换

## 1.3.23 (2026-09-03)

- 全站多语言深化落地：运行中心全量双语化（统计卡片、状态徽章、操作按钮动态翻译）
- 顶栏模型状态、账号切换器、试用横幅全面支持中英文切换
- 侧栏品牌标识与最近任务列表实现客户端即时响应无闪烁切换

## 1.3.22 (2026-09-03)

- AI 任务默认「直接跑完」：网页、桌面端、群机器人里派的任务不再每一步停下来要你确认；想盯着它一步步来的，在派发卡上选「每一步都先问我」。建发布计划、写长期记忆、配定时、拼新智能体这几样仍会停下来等你点头；对外 API 派的任务仍逐步确认
- 「采/回填我的 X（或 TikTok）账号」现在能直接派：服务端按你工作区里的账号和 handle 自动对上，派给插件去你自己的主页回填；AI 的系统提示里带上了账号清单与插件状态，不再反问你要链接。商店版插件要等下一次与桌面端同号的联合发版，解压版即时生效
- 整机版/桌面端没装采集插件时，AI 的采集任务（采竞对主页、回填自己的 X/TikTok 主页、读网页）改由本机 Chrome 当场跑完，用的是插件同一套解析器；设置页「本机权限」一眼看到电脑操作 / 浏览器操作两项的开关状态

- 全客户端与网页版国际化 (i18n)：顶栏新增全局语言切换器，全站侧栏导航、个人菜单、移动端及状态全量支持中英文双语
- 桌面客户端原生启动页（Tauri 壳）支持系统语言自动匹配与中英文切换引导
- 桌面客户端与整机版下载页面统一接入全局语言上下文

## 1.3.21 (2026-09-03)

- 群里派出的任务在飞书上有一张实时进度卡：派出时发出，之后每走一步就地更新成「正在跑 · 已走 N 步 · 最近做了什么」，跑完改成最终状态，不再往群里刷新消息；其它渠道仍在等待态与终态各回一条
- 私有化 Postgres 需执行 prisma/postgres/50-agent-run-bot-message.sql；整机版 SQLite 自动同步

## 1.3.20 (2026-09-03)

- 群里派出的任务卡在「等浏览器插件」「等额度」时也会回群说一声，不再干等
- 同一个群连发几句按顺序处理，不再丢上下文
- 「热点刷新」「合规拦截告警」两个推送事件此前从未发出过，现已接上：聚类后推新上榜热点（最多 5 条、两小时内不重复），内容被法律红线拦下时推告警；删除产品里并不存在的「竞对待刷新提醒」
- B 站作品页「读评论提问」时一并读取公开弹幕文字（只取文字，最多 100 条），进读者原声并标「弹幕」；B 站作品指标改从页面内联数据取精确值并补上评论数与发布时间。商店版插件要等下一次与桌面端同号的联合发版，解压版即时生效
- 私有化 Postgres 需执行 prisma/postgres/49-reader-comment-source.sql；整机版 SQLite 自动同步

## 1.3.19 (2026-09-02)

- 群里派出的任务，回执只发回派它的那个群，不再广播到机器人所在的所有群；微信 iLink 与微信客服派出的任务也能收到「跑完了」
- 飞书、企业微信、钉钉入站消息按消息 ID 去重，平台超时重推不再让同一句话被答两遍、同一个任务被派两次
- 企业微信自建应用回复超过上限时拆成几条按序发，不再整条丢失
- 记忆页分清「在用 / 排队中 / 被跳过」：只有真正带进每次生成的条目才标在用；新增手动记一条的入口
- 删除 B 站服务端适配器（该接口的 robots 明示禁止且需签名，此前就取不到数据）；B 站数据只走插件与 RSSHub

## 1.3.18 (2026-09-02)

- 机器人「连接渠道 / 设置机器人」弹窗改成选项卡：接入方式、默认智能体、指令权限分页，不再一长条往下滚
- 整机版更新卡片开始显示每一版改了什么（就是这份更新日志），不再只有一句结构变更计数

## 1.3.17 (2026-09-02)

- 群里直接说人话就能办事：「给我今天的选题」秒回晨报，「帮我写一篇秋季穿搭的笔记」直接派给 AI 执行器去做
- 新增 /晨报、/今日选题、/智能体 指令；「换成 XX」可以在本群切换到另一个已安装的智能体，切换后对话和派活都以它的身份进行
- 「派任务」改为默认开启（仍需绑定身份，要点头的操作照旧回站内确认）；不想让某个群派活的，在机器人设置里取消勾选
- AI 执行器：记忆写入前多三道检查（注入、祈使句、对工具能力的否定断言），防止一条脏记忆长期影响后续运行
- AI 执行器：本机命令新增底线拦截（sudo、关机、格式化盘等任何授权档都拦）；命令输出里的密钥在进入模型前自动打码
- AI 执行器：长对话压缩先剪旧的工具结果再折叠，减少「忘了前面做过什么」；新增 search_past_runs 可搜同工作区的历史运行
- 定时任务：服务重启后自动补跑停机期间漏掉的周期任务（周期 6 小时以上的，各补一次）
- 私有化 Postgres 需执行 prisma/postgres/48-bot-conversation-agent.sql；整机版 SQLite 自动同步

## 1.3.16 (2026-09-02)

- 微信个人号改走官方 iLink 机器人接口：扫码即绑，只有扫码的号能与机器人对话，不再依赖任何非官方网关
- 企业微信客服渠道重写并发与去重，连发多条不再串话
- 渠道卡实时显示用户数与群聊数，点开看每个会话的画像（群 / 私聊、发言人、消息数）
- 私有化 Postgres 需执行 prisma/postgres/47-bot-conversation-profile.sql

## 1.3.13 (2026-09-01)

- 消息渠道卡换上飞书、钉钉、企微、Telegram、Slack、微信的品牌标识；统计格改为数字在上的居中版式

## 1.3.12 (2026-09-01)

- 渠道列表补上微信说明卡：写明个人号通道为何不做、官方客服通道规划中
- 连接弹窗把长说明折进「展开」，首次接入展开、回来编辑时收起

## 1.3.11 (2026-09-01)

- 「接入 / 设置机器人」改为弹窗，不再把整页往下顶；自建应用与群 Webhook 两种接法分页签
- 弹窗里可直接选「渠道默认智能体」，编辑时回填

## 1.3.10 (2026-09-01)

- 消息渠道页按渠道一张大卡：接法、机器人数、群会话数、推送订阅、默认智能体与指令权限一目了然

## 1.3.9 (2026-09-01)

- 每个消息渠道可以绑一个智能体：群里 @机器人 时以它的职责说明开场，派活优先派它
- 私有化 Postgres 需执行 prisma/postgres/46-bot-agent.sql

## 1.3.8 (2026-09-01)

- 桌面客户端一键更新端到端跑通（下载、签名校验、原地安装、重启）
- 机器人渠道卡显示接法、订阅数、推送时刻、最近活动与报错
